{
  "name": "single-serving-sites",
  "description": "Single-Serving Sites",
  "scripts": {
    "serve": "rm -rf _site && npx @11ty/eleventy --serve --quiet",
    "build": "rm -rf _site && ELEVENTY_ENV=production npx @11ty/eleventy"
  },
  "keywords": [],
  "author": "Marcel Appelman",
  "license": "MIT",
  "browserslist": "defaults, not ie <= 11",
  "devDependencies": {
    "@11ty/eleventy": "3.0.0-alpha.5",
    "list.js": "^2.3.1"
  }
}
